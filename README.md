📊 Task Manager

This application is a simple task manager built with Python and PyQt5. It allows users to manage tasks with different priorities and provides an admin feature for adding and deleting tasks. The tasks are saved in a JSON file to maintain persistence between sessions.

🚀 Possibilities:
- User authentication with admin access.
- Ability to add, display, and delete tasks.
- Prioritization of tasks with color coding for easy identification.
- All tasks are stored in a JSON file.

⚠️ Constraints:
    pip install pyqt5

